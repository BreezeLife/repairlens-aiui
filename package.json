{
  "name": "repairlens",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm --prefix backend test && npm run validate",
    "validate": "node scripts/validate-project.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
